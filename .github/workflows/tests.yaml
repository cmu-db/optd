name: tests

on:
  pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cargo clippy --all-targets --all-features -- -D warnings
      - run: cargo test
      