# Agent Guidance

This directory contains concise operational guidance for agents working in this
repository. Keep design notes, math, and long-form investigations in `docs/`;
use `.agents/` for routing, task checklists, and recurring workflow reminders.

- `capabilities/` explains the workspace areas an agent may need to work in.
- `playbooks/` gives short, repeatable procedures for common tasks.
