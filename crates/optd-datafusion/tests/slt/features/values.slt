# VALUES support through optd ConstScan

query II
VALUES (1, 2), (3, 4);
----
1	2
3	4

query IT
SELECT * FROM (VALUES (1, 'a'), (2, 'b')) AS v(id, name) ORDER BY id;
----
1	a
2	b

query I
SELECT id
FROM (VALUES (1), (2), (3)) AS v(id)
WHERE id > 1
ORDER BY id;
----
2
3
